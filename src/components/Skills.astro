---
const skills = [
  { name: 'JavaScript / TypeScript', level: 90 },
  { name: 'React / Next.js', level: 85 },
  { name: 'CSS / Tailwind / SCSS', level: 95 },
  { name: 'Astro', level: 80 },
  { name: 'Node.js', level: 75 },
  { name: 'GraphQL', level: 70 },
];
---

<section class="py-20 bg-background relative">
  <div class="max-w-5xl mx-auto px-4">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
      
      {/* Left: Heading & Text */}
      <div>
        <h2 class="text-5xl font-comic-heading mb-6 leading-tight">
          <span class="block text-6xl text-primary drop-shadow-[3px_3px_0_#000]">POW!</span>
          My Super Powers
        </h2>
        <p class="font-comic-body text-lg mb-8">
          Every hero has their arsenal. Here are the technologies I wield to defeat bugs and deliver stunning user experiences.
        </p>
        <div class="w-32 h-32 relative">
            <svg viewBox="0 0 100 100" class="w-full h-full animate-spin-slow">
                <path id="circlePath" d="M 10, 50 a 40,40 0 1,1 80,0 a 40,40 0 1,1 -80,0" fill="none" />
                <text>
                    <textPath href="#circlePath" class="font-comic-heading text-sm tracking-widest uppercase">
                        • Coding • Designing • Deploying •
                    </textPath>
                </text>
            </svg>
            <div class="absolute inset-0 flex items-center justify-center font-comic-heading text-4xl">⚡</div>
        </div>
      </div>

      {/* Right: Skill Bars */}
      <div class="space-y-6">
        {skills.map(skill => (
          <div class="relative">
            <div class="flex justify-between mb-1">
              <span class="font-comic-heading text-lg">{skill.name}</span>
              <span class="font-comic-heading text-lg">{skill.level}%</span>
            </div>
            <div class="h-6 bg-white border-[3px] border-black shadow-[4px_4px_0_0_#000] p-[2px]">
              <div 
                class="h-full bg-primary relative group cursor-pointer" 
                style={`width: ${skill.level}%`}
              >
                  {/* Pattern Overlay */}
                  <div class="absolute inset-0 opacity-20 bg-[linear-gradient(45deg,#000_25%,transparent_25%,transparent_50%,#000_50%,#000_75%,transparent_75%,transparent)] [background-size:10px_10px]"></div>
              </div>
            </div>
          </div>
        ))}
      </div>

    </div>
  </div>
</section>

<style>
    .animate-spin-slow {
        animation: spin 10s linear infinite;
    }
    @keyframes spin {
        from { transform: rotate(0deg); }
        to { transform: rotate(360deg); }
    }
</style>
